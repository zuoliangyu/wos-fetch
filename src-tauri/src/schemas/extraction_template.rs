//! Extraction prompt template constants.
//!
//! Port target: `schemas/extraction_template.py`. Largely string literals;
//! port should preserve them verbatim so the LLM behavior stays the same.

#![allow(dead_code)]

/// Placeholder — will be populated with the Chinese-language relevance prompt
/// from the Python source.
pub const RELEVANCE_SYSTEM_PROMPT: &str = "";
